## Installation

* Install maven
```shell
$ sudo apt-get install maven
```

* Install Java
```shell
# If that doesn't work then just update
$ sudo apt-get install openjdk-21-jdk

# In case the prior command doesn't help
$ sudo apt-get update
```

* Install mysql
